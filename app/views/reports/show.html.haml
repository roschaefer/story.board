%h1 Settings for #{@report.name}
%hr
%strong Individualize your Sensor-Live-Report! Change start date and variables.
%hr
%h3 General Settings
%strong These settings determine the general look and function of your sensor-live-report.
%p
  %strong Start Date:
  = @report.start_date
%p
  %strong Video URL:
  = @report.video
  %hr
%h3 Markup Blocks
%strong Markup Blocks (Text variables) are a great way to insert custom names in your text.components. They replace markup like {exemplar} in text-components.
= render 'variables'
= link_to 'Edit all Settings', edit_report_path(@report), :class => "btn btn-primary"
= link_to 'Back', reports_path, :class => "btn btn-secondary"
