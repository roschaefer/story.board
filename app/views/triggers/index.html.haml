%h1 Triggers
%table.table.table-striped.table-hover
  %tr
    %th Heading
  - @triggers.each do |trigger|
    %tr
      %td= link_to trigger.heading, trigger_path(trigger)
=link_to 'Add new trigger', new_trigger_path, :class => "btn btn-primary"
