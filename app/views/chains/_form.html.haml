= simple_form_for(@chain) do |f|
  = f.error_notification

  .form-inputs
    = f.association :actuator
    = f.input :function, collection: Command::FUNCTIONS, include_blank: false
    = f.input :hashtag

  .form-actions
    = f.button :submit
