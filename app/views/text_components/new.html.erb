<%= form_for :text_component, url: text_components_path do |f| %>

 <% if @text_component.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@text_component.errors.count, "error") %> prohibited this text component from being saved:
      </h2>
      <ul>
        <% @text_component.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <p>
    <%= f.label :heading %><br>
    <%= f.text_field :heading %>
  </p>
 
  <p>
    <%= f.label :introduction %><br>
    <%= f.text_area :introduction %>
  </p>
  <p>
    <%= f.label :main_part %><br>
    <%= f.text_area :main_part %>
  </p>
  <p>
    <%= f.label :closing %><br>
    <%= f.text_area :closing %>
  </p>
 
  <p>
    <%= f.submit %>
  </p>
<% end %>

<%= link_to 'Back', text_components_path %>
