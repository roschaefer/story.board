%table.table.item-table
  %thead.thead-default
    %tr
      %th{width: '40%'} Trigger
      %th
      %th{width: '40%'} Headline
      %th Start
      %th End
      %th
  %tbody
    - @trigger_groups.each do |triggers, text_components|

      - text_components.each.with_index do |text_component, index|
        %tr.item-table__item
          -if index === 0
            %td{rowspan: text_components.count}
              .item-table__title
                .item-table__title__clip{title: triggers.count > 0 ? triggers.map(&:name).join(', ') : 'Text Components without Trigger', data: {toggle: 'tooltip'}}
                  %strong
                    - if triggers.count > 0
                      %span=triggers.map(&:name).join(', ')
                    - else
                      Text Components without Trigger

          = render 'table_row', text_component: text_component